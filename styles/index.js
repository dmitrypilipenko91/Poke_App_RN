import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
      flex: 1,
      backgroundColor: '#fff',
      alignItems: 'center',
      justifyContent: 'center',
    },
    counter: {
      flexDirection: 'row'
    },
    pokeBlock: {
      borderStyle: 'solid',
      borderWidth: '1px',
      marginTop: '1px'
    }
  });